import React from "react";

interface CardProps {
  cards: [];
  lastFlip: boolean;
  selected: object;
  selectedCard: () => {};
}

const Cards = ({ cards, lastFlip, selected, selectedCard }: CardProps) => {
  return null;
};

export default Cards;
